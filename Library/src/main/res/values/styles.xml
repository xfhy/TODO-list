<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/color_949494</item>

    </style>

    <!-- 加载弹框样式 -->
    <style name="LightDialog" parent="@android:style/Theme.Dialog">
        <!--提示框是否有边框-->
        <item name="android:windowFrame">@null</item>
        <!--提示框是否是浮动的-->
        <item name="android:windowIsFloating">true</item>
        <!--是否有遮盖-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--软键盘调整-->
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan
        </item>
    </style>

    <style name="LightProgressDialog" parent="@style/LightDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- TabLayout文字样式-->
    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/font_size_16</item>
    </style>

    <!-- TabLayout通用样式-->
    <style name="TabsStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="tabIndicatorColor">@color/common_white</item>
        <item name="tabBackground">@drawable/tab_selector</item>
        <item name="tabTextAppearance">@style/TabLayoutTextStyle</item>
        <item name="tabTextColor">@color/text_light_dark</item>
        <item name="tabSelectedTextColor">@color/common_white</item>
    </style>

    <!-- 横向分隔线 -->
    <style name="HorizontalDivider">
        <item name="android:background">@color/common_divider</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/common_divider_height</item>
    </style>

    <!-- 竖向分隔线 -->
    <style name="VerticalDivider">
        <item name="android:background">@color/common_divider</item>
        <item name="android:layout_width">@dimen/common_divider_width</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- 竖向分隔线 带上下间距 -->
    <style name="DividerFixedWidth.MarginTopBottom" parent="@style/VerticalDivider">
        <item name="android:layout_marginTop">@dimen/common_margin_middle</item>
        <item name="android:layout_marginBottom">@dimen/common_margin_middle</item>
    </style>

    <!-- 底部出入动画 -->
    <style name="AnimBottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <!-- 横屏按钮样式，带不可用状态 -->
    <!-- 按钮样式-->
    <style name="HorizontalBtnStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/common_white</item>
        <item name="android:textSize">@dimen/font_size_14</item>
        <item name="android:background">@drawable/btn_common_selector</item>
        <item name="android:padding">@dimen/common_padding_middle</item>
        <item name="android:layout_margin">@dimen/common_margin</item>
        <item name="android:enabled">false</item>
    </style>

    <style name="stfLayoutContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="stfLayoutProgress">
        <item name="android:layout_width">@dimen/dimen_60</item>
        <item name="android:layout_height">@dimen/dimen_60</item>
    </style>

    <style name="stfLayoutImage">
        <item name="android:layout_width">@dimen/dimen_80</item>
        <item name="android:layout_height">@dimen/dimen_80</item>
        <item name="android:tint">@color/stfImageColor</item>
    </style>

    <style name="stfLayoutMessage">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/dimen_20</item>
        <item name="android:layout_marginRight">@dimen/dimen_20</item>
        <item name="android:layout_marginTop">@dimen/dimen_10</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="stfLayoutButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/dimen_10</item>
        <item name="android:text">@string/empty_view_retry</item>
        <item name="android:background">@drawable/shape_empty_view_retry</item>
        <item name="android:textColor">@color/color_bfbfbf</item>
        <item name="android:textSize">@dimen/font_size_18</item>
    </style>

    <!-- 通用Dialog使用-->
    <style name="CommonDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
